# AGENTS.md

## 语言要求

- 所有面向用户的回复必须使用中文。
- 所有计划必须使用中文。
- 仓库内的提交说明与可复现记录必须使用中文；命令、路径和工具原始输出保持原样。

## Python 与依赖管理

- 执行任何 Python 命令前，必须先检查工作区是否存在 `uv`、`uv.lock` 或由 `uv` 管理的 `pyproject.toml`。
- 本仓库统一使用 `uv` 作为唯一的 Python 运行器和包管理器。
- 使用 `uv run` 执行 Python 脚本，禁止直接使用 `python` 或 `python3`。
- 使用 `uv add` 或 `uv pip install` 管理依赖，禁止直接使用 `pip`。
- 如需创建虚拟环境，使用 `uv venv`，禁止使用 `python -m venv`。
- 每次修改仓库文件后、提交前，必须运行 `uv lock` 并确认成功；如依赖发生变化，必须一并提交更新后的 `uv.lock`。

## 提交与可复现性

- 每个提交必须说明用户的指令原文。
- 每个提交必须说明将用户指令形式化后的执行计划。
- 每个提交必须记录实际运行的命令及其结果，包括失败命令。
- 每个提交必须保证其他人能够依据记录复现。
- 每个提交在 `docs/commits/` 中维护一份对应的中文记录，文件名应能对应提交名称或编号。
- Git 提交信息必须包含简短标题，并在正文中包含“用户指令原文”“形式化计划”“运行命令与结果”三个部分。
- 禁止伪造未运行的命令或结果；无法在提交前写入文件的提交命令结果，应保留在 Git 提交信息或后续可审计记录中。

## Git 与远程仓库

- 公开仓库内容不得包含令牌、密码、私钥、个人访问凭据或其他敏感信息。
- 推送前必须检查 `git status`、待提交文件和远程仓库配置。
